@page
@model Rental.Pages.Tenancies.IndexModel

@{
    ViewData["Title"] = "Index";
}

@section Styles {
    <!-- DataTables -->
    <link rel="stylesheet" href="~/lib/dataTables/dataTables.bootstrap4.min.css">
}

@section Scripts {
    <!-- DataTables -->
    <script src="~/lib/dataTables/jquery.dataTables.min.js"></script>
    <script src="~/lib/dataTables/dataTables.bootstrap4.min.js"></script>

    <!-- page script -->
    <script>
        $(function () {
            $('#MainGrid').DataTable({
                'paging': true,
                'lengthChange': true,
                'searching': true,
                'ordering': true,
                'info': true,
                'autoWidth': true
            })
        })
    </script>
}

<h1>Tenancies</h1>

<p>
    <a asp-page="Create" class="btn btn-primary" data-toggle="tooltip" data-placement="top" accesskey="n" title="[ALT]+N">Create New</a>
</p>
<table class="table table-striped table-bordered" id="MainGrid">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].BookingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].FromDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].ToDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].AdvanceAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].Unit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].Party)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenancy[0].UnitPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Tenancy)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BookingDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FromDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ToDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AdvanceAmount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Unit.Code)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Party.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UnitPrice.Id)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Modify</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
